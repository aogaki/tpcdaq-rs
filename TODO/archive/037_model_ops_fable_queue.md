# 037 — モデル運用細目: Fable キュー + スポット Fable(週制限下の Opus 主対話体制)

**Status: COMPLETED**(2026-08-14 主対話セッションで直接実施 — ドキュメントのみ)

## 結果

- **変更は CLAUDE.md と TODO/CURRENT.md の 2 ファイルのみ**(コード無変更。cargo test 対象外
  = md のみなので自明。035 の前例に倣う)。
- CLAUDE.md「モデル使い分け」節に運用細目を追記:
  ① **Fable キュー** — Opus セッション中に出た設計判断・SPEC 疑義・レビュー依頼は
  CURRENT.md「Fable 待ち」節に積み、Fable セッション 1 回でバッチ消化。Opus は SPEC の
  diff 案まで、**確定は Fable**。
  ② **スポット Fable** — Agent `model: "fable"` のサブエージェント一発投げを許可。ただし
  入力を自己完結にパッケージできる仕事(フェーズ境界レビュー = diff + テスト出力 + 関係
  SPEC 節)に限る。会話文脈を持たないためオープンエンド設計対話には使わない。
  ③ **エスカレーションの二段化** — チケット不備の一次対応は主対話(Opus)、割れる /
  SPEC 解釈に踏み込むなら Fable キュー行き(旧記述「修正は Fable の仕事」を置換)。
  ④ **滞空時間の緩和** — Fable 非常駐の間はフェーズを小さく保ち、レビューはフェーズ境界ごと。
- TODO/CURRENT.md に「Fable 待ち(キュー)」節を新設(現在は空)+ 完了台帳に 037 を追記。
- 実行環境・日付: macOS Darwin 25.5.0、2026-08-14。テスト実行なし(理由: md のみの変更)。

## 背景

- 週間制限がきつく、主対話モデルを Fable → Opus に切り替えたい(2026-08-14 ユーザー相談)。
- 既存の「主対話が Opus でも同じ体制」(2026-08-14 決定)は骨格のみで、Fable の消費形態
  (常駐 → 点)の運用手順が未定義だった。
- 方針: **判断軸(設計の裁量が残る仕事 = 上位モデル)は変えず、Fable を「対話の相手」から
  「フェーズ境界と SPEC にだけ現れる査読者」に降格**。消費をバッチ化して週制限内に収める。

## やったこと

- 上記 ①〜④ を CLAUDE.md に、キュー実体を CURRENT.md に反映(本文は結果節のとおり)。

## 受け入れ

- ファイル所有権: CLAUDE.md / TODO/CURRENT.md / 本チケットのみ。コード・SPEC に触らない。
